module <%= namespace %>
  class Client < Smithy::Client::Base
    def self.new: (
<% option_types.each do |name, type| -%>
      ?<%= name %>: <%= type %>,
<% end -%>
    ) -> void
    | (?Hash[Symbol, untyped]) -> void

<% operations.each do |operation| -%>

    def <%= operation.name %>: (?Hash[Symbol, untyped], ?Hash[Symbol, untyped]) -> Smithy::Client::_Output[untyped]
<% end -%>
  end
end
